self.addEventListener('install',()=>self.skipWaiting());self.addEventListener('activate',e=>e.waitUntil(self.clients.claim()));self.addEventListener('push',e=>{let d={title:'🍽️ ARGO · NUOVO ORDINE',body:'È arrivato un nuovo ordine',url:'./orders.html'};try{if(e.data)d={...d,...e.data.json()}}catch(_){}e.waitUntil((async()=>{await self.registration.showNotification(d.title,{body:d.body,icon:'../admin-icon.svg',badge:'../admin-icon.svg',tag:d.orderId?'argo-'+d.orderId:'argo-order',renotify:true,data:{url:d.url||'./orders.html'}});if(self.registration.setAppBadge)try{await self.registration.setAppBadge()}catch(_){}})())});self.addEventListener('notificationclick',e=>{e.notification.close();const target=new URL(e.notification.data?.url||'./orders.html',self.location.href).href;e.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(ws=>{for(const w of ws){if('focus'in w){w.navigate(target);return w.focus()}}return clients.openWindow(target)}))});